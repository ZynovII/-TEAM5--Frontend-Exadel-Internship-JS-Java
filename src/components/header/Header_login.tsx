import { FC } from 'react';
import { DefaultButton } from '@fluentui/react/lib';

const ButtonLog: FC = () => {
  return (
    <DefaultButton text="Log In"  allowDisabledFocus />
  );
};

export default ButtonLog